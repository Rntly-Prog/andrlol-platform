<% const score = typeof rating === 'number' ? Math.round(rating) : 0; %>
<div class="stars" aria-label="Оценка <%= score %> из 5">
  <% for (let i = 1; i <= 5; i++) { %>
    <span class="star <%= i <= score ? 'star--filled' : '' %>">★</span>
  <% } %>
</div>
